export const vertexShader=`varying vec3 vWorld;void main(){vWorld=(modelMatrix*vec4(position,1.)).xyz;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`;
export const fragmentShader=`precision mediump float;uniform sampler2D uEarth;uniform float uHeat;uniform float uYear;uniform vec3 uLight;varying vec3 vWorld;void main(){vec3 P=normalize(vWorld);const float PI=3.14159265359;vec2 uv=vec2(atan(P.z,P.x)/(2.*PI)+.5,asin(clamp(P.y,-1.,1.))/PI+.5);vec3 earth=texture2D(uEarth,uv).rgb;float light=.28+.72*max(dot(P,normalize(uLight)),0.);float heatWindow=smoothstep(-300000.,-10000.,uYear);heatWindow=mix(heatWindow,1.,smoothstep(-10000.,2026.,uYear));float pattern=pow(max(0.,sin(P.x*17.)*sin(P.z*19.)),10.);float population=heatWindow*(.35+.65*(1.-abs(P.y)))*pattern*uHeat;vec3 heat=mix(vec3(.04,.25,1.),vec3(1.,.02,.01),smoothstep(.08,.72,population));earth=mix(earth,heat,population*.72);gl_FragColor=vec4(earth*light,1.);}`;
export const atmosphereVertex=`varying vec3 vNormal;varying vec3 vView;void main(){vNormal=normalize(normalMatrix*normal);vec4 mv=modelViewMatrix*vec4(position,1.);vView=normalize(-mv.xyz);gl_Position=projectionMatrix*mv;}`;
export const atmosphereFragment=`precision mediump float;varying vec3 vNormal;varying vec3 vView;void main(){float rim=pow(1.-max(dot(vNormal,vView),0.),3.2);gl_FragColor=vec4(.12,.55,1.,rim*.5);}`;
